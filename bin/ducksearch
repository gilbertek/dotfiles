#!/usr/bin/env python

from sys import argv
from webbrowser import open_new_tab

duck = "https://duckduckgo.com/?q="

if len(argv) == 1:  # SCRIPT NAME:
    print("Enter search term.")
else:
    open_new_tab(duck + "".join(argv[1:]))
